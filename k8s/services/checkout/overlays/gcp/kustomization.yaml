apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

generatorOptions:
  disableNameSuffixHash: true

images:
  - name: checkout-image
    newName: europe-west1-docker.pkg.dev/PROJECT_ID/apps/checkout
    newTag: v1.0.0

configMapGenerator:
  - name: checkout-config
    namespace: apps
    literals:
      - CLOUD_PROVIDER=gcp
      - SERVICE_NAME=checkout

patches:
  - path: serviceaccount-patch.yaml
