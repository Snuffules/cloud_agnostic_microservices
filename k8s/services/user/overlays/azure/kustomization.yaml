apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

generatorOptions:
  disableNameSuffixHash: true

images:
  - name: user-image
    newName: ACR_NAME.azurecr.io/user
    newTag: v1.0.0

configMapGenerator:
  - name: user-config
    namespace: apps
    literals:
      - CLOUD_PROVIDER=azure
      - SERVICE_NAME=user

patches:
  - path: serviceaccount-patch.yaml
  - path: deployment-patch.yaml
