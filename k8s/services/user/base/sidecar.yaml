apiVersion: networking.istio.io/v1
kind: Sidecar
metadata:
  name: user
  namespace: apps
spec:
  workloadSelector:
    labels:
      app.kubernetes.io/name: user
  egress:
    - hosts:
        - "./*"
        - "istio-system/*"
